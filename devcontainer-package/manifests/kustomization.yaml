apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply in dependency order.
# For per-user namespaces use provision-workspace.sh instead of applying this directly.
resources:
  - namespace.yaml
  - limit-range.yaml
  - resource-quota.yaml
  - pvc.yaml
  - dev-workspace-deployment.yaml
